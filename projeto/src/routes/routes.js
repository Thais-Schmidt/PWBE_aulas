const express = require("express");
const router = express.Router();

const controller = require("../views/controllers/controller");

// router.get("/", controller.index)
// router.get("/cadastro", controller.cadastro)
// router.get("")













module.exports = router;